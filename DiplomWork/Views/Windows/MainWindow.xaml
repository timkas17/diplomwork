<Window x:Class="DiplomWork.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DiplomWork.ViewModels"
        xmlns:local="clr-namespace:DiplomWork"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <ToolBar HorizontalAlignment="Center" Height="52" VerticalAlignment="Top" Width="800" Padding="5">
            <Image Height="30" Width="30"/>
        </ToolBar>

        <Grid>
            <Frame x:Name="MainFrame" Content="{Binding CurrentPage}"/>
        </Grid>
    </Grid>
</Window>
